apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default-mtls
  namespace: titanium-prod
spec:
  mtls:
    mode: PERMISSIVE  # Temporary: STRICT -> PERMISSIVE for Ingress Gateway compatibility
