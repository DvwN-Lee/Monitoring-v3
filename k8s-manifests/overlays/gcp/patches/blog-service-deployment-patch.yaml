apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-service-deployment
spec:
  template:
    metadata:
      annotations:
        # Exclude PostgreSQL port from Istio proxy
        traffic.sidecar.istio.io/excludeOutboundPorts: "5432"
    spec:
      containers:
      - name: blog-service-container
        env:
        # Add PostgreSQL environment variables
        - name: SERVICE_NAME
          value: "blog-service"
        - name: SERVICE_PORT
          value: "8005"
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_PORT
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_DB
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_SSLMODE
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_SSLMODE
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: REDIS_PASSWORD
        # Remove BLOG_DATABASE_PATH (SQLite)
