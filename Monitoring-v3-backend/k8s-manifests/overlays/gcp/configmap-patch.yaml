apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Database Configuration
  USE_POSTGRES: "true"

  # PostgreSQL Configuration
  POSTGRES_HOST: "postgresql-service"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "titanium"

  # Service URLs (with prod- prefix)
  API_GATEWAY_URL: "http://prod-api-gateway-service:8000"
  USER_SERVICE_URL: "http://prod-user-service:8001"
  AUTH_SERVICE_URL: "http://prod-auth-service:8002"
  BLOG_SERVICE_URL: "http://prod-blog-service:8005"

  # Redis Configuration
  REDIS_HOST: "prod-redis-service"
  REDIS_PORT: "6379"
  REDIS_DB: "0"

  # Environment Settings
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  DEBUG_MODE: "false"

  # Application Info
  APP_NAME: "titanium"
  APP_VERSION: "2.0.0"

  # CORS Settings
  # Production 환경에서는 특정 Domain만 허용
  CORS_ALLOWED_ORIGINS: "https://titanium.example.com,https://admin.titanium.example.com"
